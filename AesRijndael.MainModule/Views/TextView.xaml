<UserControl x:Class="AesRijndael.MainModule.Views.TextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AesRijndael.MainModule"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="{local:Localization TextTitle}">
        <Grid>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Margin="10" Grid.Column="0">
                <TextBlock Text="{local:Localization EncryptedText}"/>
                <RichTextBox IsReadOnly="{Binding IsEncryptionReadOnly}" />
            </StackPanel>

            <StackPanel Margin="10" Grid.Column="1">
                <TextBlock Text="{local:Localization PlainText}"/>
                <RichTextBox IsReadOnly="{Binding IsDecryptionReadOnly}"/>
            </StackPanel>
        </Grid>
    </GroupBox>
</UserControl>
